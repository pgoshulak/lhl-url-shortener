<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
<ul>
  <% 
    var keys = Object.keys(urls);
    for (key of keys) { %>
      <li><%= key %> :: <%= urls[key] %>
    <% }
  %>
</ul>
</body>
</html>