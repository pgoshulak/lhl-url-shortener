<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
<% include ./partials/_header %>
<ul>
  <% for (key in urls) { %>
      <li><%= key %> :: <%= urls[key] %>
    <% }
  %>
</ul>
</body>
</html>